% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustrfusion.R
\name{clustrfusion}
\alias{clustrfusion}
\title{Fusion of clusters based on shared cluster enrichment}
\usage{
clustrfusion(clustrenrich_data, monoterm_fusion = FALSE)
}
\arguments{
\item{clustrenrich_data}{The named \code{list} output from the \code{clustrenrich()} function.}

\item{monoterm_fusion}{Option to merge clusters only if they either enrich the same total terms (monoterm_fusion = FALSE) or the same individual term (monoterm_fusion = TRUE) in at least one source (default set to FALSE).}
}
\value{
A named \code{list} holding 4 components, where :
-\code{dr_g_a_fusion} is a dataframe of type \emph{g_a} holding the cluster fusion results. It shares a similar structure to the \emph{clustrenrich_data$dr_g_a_enrich} dataframe with each row being a combination of gene and biological function annotation.
-\code{dr_c_a_fusion} is a dataframe of type \emph{c_a} holding the cluster fusion results with each row being a combination of cluster ID and biological function annotation
-\code{c_fusionlog} is a dataframe tracing cluster fusion events, indicating the sources from which they originated (e.g. GO, KEGG).
-\code{params} is a list of the main parameters used; in this case monoterm_fusion
}
\description{
This function fusions clusters that share the same biological function enrichment in at-least one of the sources used (e.g. GO, KEGG...).
}
\examples{
\dontrun{
# Merge clusters based on shared functional enrichment
# Requires output from clustrenrich()

fusion_results <- clustrfusion(
  clustrenrich_data = your_clustrenrich_res,
  monoterm_fusion = FALSE  # Allow fusion based on all shared terms
)

# Compare original vs fused cluster assignments
table(fusion_results$dr_t_c_a_fusion$old_clustr, 
      fusion_results$dr_t_c_a_fusion$new_clustr)
}
}
